pipeline 
{
    environment
    {
    registry = "dilberzaman/cmd-angular"
    registryCredential = "dockerhub"
    dockerImage = ''
    }
  agent any
  stages 
  {
     stage('Sonarqube'){
      steps{
        echo "Sonarqube codequality"
        sh '''
            ${scannerHome}/bin/sonar-scanner \
              -Dsonar.projectKey=cmd-angular \
              -Dsonar.sources=. \
              -Dsonar.host.url=http://52.45.81.194:9000 \
              -Dsonar.login=sqp_0aed6a8a594289d4f6944089d55b28ff44e62a54
          '''
      }
    }
     stage('Build docker image')
     {
      steps
      {
        echo "Building docker image"
        script
        {
          dockerImage = docker.build registry + ":$BUILD_NUMBER" 
        }
      }
     }

     stage('Push docker image')
     {
      steps
      {
        echo "Pushing docker image"
        script
        {
           docker.withRegistry('',registryCredential) 
           {
            dockerImage.push()
            dockerImage.push('latest')
           }
        }
      }      
     }
   stage("deploy k8")
    {
      steps
      {
        script
        {
         kubernetesDeploy(configs: "deployment.yaml", kubeconfigId: "KUBERNETES_CLUSTER")
        }
      }
    }
  }
}  
 
